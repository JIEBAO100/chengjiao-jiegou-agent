# 成交结构拆解智能体 · 项目说明

**参赛方向：数据分析**

**建议仓库名：`chengjiao-jiegou-agent`**

---

## 一、一句话介绍

实时抓取交易所公开逐笔成交并「滚雪球」积累，先把成交量按金额拆成**大单 / 中单 / 小单**，
再用十二个订单流特征量给**主力吃货 / 主力出货 / 散户乱交易**三种资金行为打分，
最后判断当前盘面属于**主力真实吸筹 / 主力诱多出货 / 纯散户行情无主力**哪一种。

> 不预测涨跌，只回答一个问题：**这些成交，是谁在打？**

---

## 二、项目背景：价格看不出「谁在买」

看K线只能看到价格走到了哪里，看不到成交是怎么打出来的。

同样是上涨 2%，可能是主力在持续吸筹（真金白银在买，卖压被一点点吃掉），
也可能是主力一边拉抬一边出货（散户在往上接）。这两种形态在价格图上几乎一样，
在成交结构上却完全不同。要区分它们，必须回到逐笔成交这一层。

三个现实困难，本项目逐一解决：

| 困难 | 本项目的做法 |
|---|---|
| 公开接口只给最近约 1000 条逐笔，没有历史 | 页面每 2 秒轮询一次，按成交编号去重后「滚雪球」累加；漏单如实检测并标记 |
| 「大单」没有统一标准，1 万美元的 BTC 单是小单、放到冷门币上就是巨单 | 分档线跟着该币种自身的成交分布走（中位数倍数 + 分位护栏 + EMA 平滑） |
| 算法拆单会把一笔大单伪装成几百笔小单 | 逐笔之上再做「策略单聚合」（同向 + 3 秒内 + 价格贴合），订单级分布单独统计 |

---

## 三、核心功能

### 3.1 大小单拆解（把成交量拆成三档）

分档尺子（**所有币种通用，不写死金额**）：

```text
中单线 = max(单笔成交额中位数 × 3, P75)
大单线 = max(单笔成交额中位数 × 10, P95)
两条线至少拉开 2 倍（否则中单会被压成一条缝）
阈值做 EMA 平滑（系数 0.2），避免每来一批数据就换线
```

> **为什么中单线用 P75 而不是更高分位**：币圈的成交分布常见「海量碎单 + 少量大单」的双峰形态。
> 如果中单线用 P95，它会被直接顶到大单量级，结果所有大单都掉进中单里、大单占比恒为 0，
> 吸筹/出货这类结论就永远出不来。**这个缺陷是本项目自测里真实抓到的**，修完才成为现在这版。

页面展示每档的：成交额、占比、主动买 / 主动卖、净额、笔数，以及三档占比堆叠条。

### 3.2 三种资金行为打分

从逐笔里提取**十二个订单流特征量**，再映射成三个 0–100 的分数：

| 分数 | 主要证据 |
|---|---|
| **主力吃货** | 大单净买入；买盘吸收顺畅（同样的成交只推动很小的价格变化）；资金在时间上扎堆；大单在吸、小单在抛 |
| **主力出货** | 大单净卖出；**价格在涨、大单却在净卖**（拉抬中派发）；散户追涨明显；大单在派发、小单在接 |
| **散户乱交易** | 大单占比低（主力级资金缺席）；成交额主要来自碎单；大单买卖基本均衡、没有清晰方向 |

十二个特征量：大单净流入占比（NBI）、大单成交占比、主动方向纯度、价格冲击弹性（λ）、
时间集中度、大单占比漂移、大小单背离、散户追涨度、资金与价格背离（CVD）、小单主导度、
节奏稳定性、中单占比。

### 3.3 盘面三态判定

三种分数取最高分作为结论：

| 结论 | 含义 |
|---|---|
| **主力真实吸筹** | 大单持续站在买方，价格却没有同步走强 —— 卖压被资金默默吸收 |
| **主力诱多出货** | 大单站在卖方而价格仍在走强 —— 筹码正由主力流向散户 |
| **纯散户行情无主力** | 成交以小额成交为主，缺乏主力级资金的持续痕迹 |

两条诚实规则：

- **结构模糊**：三个分数咬得很紧（领先不足 8 分）时，宁可说不知道，也不硬给结论；
- **数据积累中**：样本不足 300 笔、或已完结时间桶少于 6 个时，明确说明在积累，不拿半截数据硬凑。

另外，结论切换有 **10 分迟滞**：新结论要明显领先旧结论才切换，避免页面上判定结果来回跳动。

### 3.4 逐笔采集质量自校验

- **覆盖率** = 逐笔累计成交额 ÷ 同期K线成交额，只统计完整跟踪过的**已完结**桶；
- **采集缺口**：两次轮询之间漏掉的成交会被检测出来（编号不连续），涉及的桶打上「不完整」标记；
- 页面上一格一格显示每个时间桶的采集进度，**不完整的桶一眼能看出来**。

---

## 四、页面结构

```text
品牌栏（项目名 + 参赛标识 + 运行状态）
 ├─ 主视觉：一句话说明这个工具做什么
 ├─ 控制台：市场 / 币种搜索 / 周期 / 观察窗 / 采集开关
 ├─ 错误提示（只在真的出错时出现，写明中文原因）
 └─ 结果区
     ├─ 行情摘要卡（最新价 / 涨跌 / 成交额 / 持仓量 / 资金费率 / 标记价）
     ├─ 逐笔采集状态（样本、覆盖率、缺口、通道、时间桶进度条）
     ├─ K线与成交结构（Canvas 自绘，鼠标悬浮看每根K线明细）
     ├─ 成交结构拆解（大单 / 中单 / 小单三卡 + 占比条 + 分档尺子说明）
     ├─ 主力行为研判（结论徽章 + 三条分数条 + 支持证据 / 反对证据）
     └─ 成交结构时间轴（每个桶的堆叠柱 + 大单净额折线）
```

**视觉风格**：奶白底（羊皮纸感）+ 红牛强调色，网格纹理与金色点缀。
按中国习惯，**上涨用红、下跌用绿**；大单用深红、中单用金、小单用灰蓝。

---

## 五、数据来源与口径

| 项目 | 说明 |
|---|---|
| 数据来源 | 各大交易平台公开行情接口（K线、逐笔成交、合约信息） |
| 是否需要密钥 | 不需要，全部是公开行情接口 |
| 永续逐笔金额换算 | 张数 × 合约乘数 × 成交价（乘数来自合约信息接口） |
| 现货逐笔金额换算 | 数量 × 成交价 |
| 方向口径 | 永续用成交张数正负号；现货用成交记录的买卖标记 |
| 是否需要账户 | 不需要，不登录、不绑定、不读取任何账户信息 |
| 是否涉及交易 | 不涉及，不接交易权限、不保存私钥、不执行下单 |

**数据真实性原则**：页面只展示接口真实返回的数据。接口失败或超时时，显示明确的中文失败原因，
**不会用演示数据或固定数值顶替，也不会把演示数据描述成实时数据**。

---

## 六、技术实现

### 6.1 双通道取数

| 数据类型 | 优先通道 | 兜底通道 | 原因 |
|---|---|---|---|
| K线 / 币种列表 | 云端接口代理（Netlify Functions，带缓存） | 浏览器直连 | 低频，走服务端更稳、更省流量 |
| 逐笔成交（2 秒级） | 浏览器直连 | 云端接口代理 | 高频，走云端会很快耗尽免费额度 |

两条通道取到的都是真实数据，**归一化口径只在 `trade-store.js` 一处维护**，所以两条通道结果一致。
页面顶部如实显示当前实际使用的通道。云端接口返回 404 / 非 JSON / 缺少成功标记时，
判定为「没有部署云端函数」，自动切到直连，**不会白屏**。

### 6.2 性能设计

- 逐笔轮询 2 秒一次，失败时指数退避（最长 30 秒一次），恢复后自动回到 2 秒；
- 页面切到后台自动暂停轮询（省流量、省接口压力），回到前台自动继续；
- 环形缓冲：最多保留 2 万条逐笔 + 240 个时间桶，超限从头部裁剪；
- 结构分析有最小间隔节流（900 毫秒），避免高频轮询把主线程压满。

### 6.3 技术选型

| 部分 | 实现 | 原因 |
|---|---|---|
| 分析算法 | 纯 JavaScript 前端计算 | 全部基于真实数据，不使用任何预置数值 |
| K线图 | 原生 Canvas | 零第三方库、零 CDN，避免加载失败导致白屏 |
| 成交结构时间轴 | 原生 Canvas | 同上 |
| 时间桶进度条 | 纯 CSS | 上百个格子也能快速渲染 |
| 币种搜索 | 原生实现（输入即筛选 + 键盘操作） | 无需任何下拉组件库 |
| 本地调试 | 自建 dev-server | 复用与线上完全相同的函数代码 |

### 6.4 开发过程中发现并修复的真实缺陷

以下问题都是在**真实行情 + 真实浏览器环境**的自测中发现的，不是看代码看出来的：

| 缺陷 | 现象 | 修复 |
|---|---|---|
| 中单线护栏过高 | 成交分布是「大量碎单 + 少量大单」时，大单全被划进中单，大单占比恒为 0，吸筹/出货结论永远出不来 | 中单线改用 P75 护栏（原先用 P90/P95 会顶到大单量级） |
| 直方图整箱划线 | 分档阈值所在的那一箱被整体划到低档，大单占比被系统性低估 | 临界箱内按对数均匀按比例切分 |
| 页面调用未定义函数 | 每次采集成功都会抛 `notifyUI is not defined` | 移除该调用，改为统一的分析刷新入口 |
| 币种切换后旧监听残留 | 换掉下拉框后旧的事件监听会直接抛错 | 改为绑定新的搜索框交互 |
| 币种列表只给前 200 个 | 想分析冷门币根本搜不到 | 上限提到全量（永续约 980、现货约 2000），并做成本地搜索 |
| 默认周期过长 | 15 分钟档要等一个多小时才有结构结论 | 默认改为 1 分钟档，约 6 分钟出结果 |
| 关键词匹配不精确 | 输入 `DOGE` 命中多个同名变体时回车不选 | 完全匹配优先 |
| 本地服务器暴露配置文件 | `package.json` 能通过网页直接下载 | 拦截清单补上 `.json` |
| 逐笔脏数据参与统计 | 价格为 NaN 的记录会污染价格序列 | 引擎入口过滤掉时间/价格/金额不完整的记录 |

---

## 七、自测与验证

| 测试 | 覆盖内容 | 规模 |
|---|---|---|
| `_test-core.mjs` | 分档阈值、三态判定（吸筹/出货/散户三种典型形态）、冷启动门槛、结果内部一致性、逐笔仓库（去重 / 缺口 / 覆盖率）、异常与离群值 | 33 项 |
| `_test-server.mjs` | 首页与全部静态资源、三个接口的返回结构、全量币种规模、失败路径的中文原因、访问控制、品牌名合规 | 44 项 |
| `_test-page.mjs` | jsdom 真实加载页面：脚本无报错、全量币种加载、搜索的输入/筛选/鼠标选中/键盘选中、两块新面板渲染、交互控件、合规底线 | 42 项 |
| `_test-chart.mjs` | 图表绘制：用记录型 2D 上下文验证 K线与堆叠柱真的画出来了、坐标不含 NaN、空数据与异常值不崩 | 24 项 |
| `_test-page-restore.mjs` | 本地缓存恢复后立即出结论：三档拆解数字、占比合计 100%、三态结论为「主力真实吸筹」、三条分数条、证据栏、置信度 | 25 项 |

五套测试合计 **168 项全部通过**，上传前另有格式体检脚本逐项核对（两种模式各 15 项）。

---

## 八、项目亮点（评审视角）

1. **视角落地到逐笔** —— 不做又一根均线，而是回到成交本身，回答「谁在打」这个K线回答不了的问题。
2. **分档不是拍脑袋** —— 阈值跟着每个币种自己的分布走，还用 P75/P95 护栏 + EMA 平滑，
   换任何币种都成立；双峰分布下的阈值陷阱是实测发现并修掉的。
3. **结论可复核** —— 三条行为分数、十二个特征量、支持证据与反对证据全部展示在页面上，
   每条结论都能查出依据，不是黑盒。
4. **该说不知道就说不知道** —— 样本不足判「数据积累中」，分数咬得紧判「结构模糊」；
   采样有缺口如实标记；覆盖率对不上就显示对不上。
5. **工程上真扛得住** —— 2 秒轮询 + 指数退避 + 后台暂停 + 环形缓冲 + 本地缓存恢复，
   长时间开着不会越跑越卡。
6. **零外部依赖** —— 图表自绘、算法自算、无 CDN，加载失败风险低。
7. **部署自由** —— 同一套代码既能配合云端函数（带服务端缓存），也能纯静态直接上线（自动切直连）。

---

## 九、风险提示

本工具仅用于市场数据观测与结构分析，**不构成任何投资建议**，不代表任何买卖承诺。
页面中的大小单分档、行为分数与三态结论，均为基于公开逐笔成交的程序化统计结果，**不等于实际交易建议**。

需要特别说明的几点：

- **三态判定描述的是当期结构**，不代表后续涨跌；主力吸筹与主力出货都可能在极短时间内反转；
- **「主力」是统计意义上的大额资金**，判断依据是成交金额分布与订单流特征，不等于识别出某个具体主体；
- **逐笔样本是滚雪球积累的**：页面刚打开时样本不足，结论会显示「数据积累中」，这是设计上的诚实行为；
- **数据可能存在延迟、缺失或接口波动**，页面会在失败时如实报错，不会掩盖。

市场有风险，投资需谨慎。任何投资决策应结合个人风险承受能力、资金状况与投资目标独立判断，
必要时咨询持牌专业机构。过往表现不预示未来收益。
